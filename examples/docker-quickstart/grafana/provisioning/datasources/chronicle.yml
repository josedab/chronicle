apiVersion: 1

datasources:
  - name: Chronicle
    type: prometheus
    access: proxy
    url: http://chronicle:8086
    isDefault: true
    editable: false
    jsonData:
      httpMethod: POST
      timeInterval: "15s"

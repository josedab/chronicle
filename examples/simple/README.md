# Simple Example

Basic usage of Chronicle: open a database, write data points, and query with aggregation.

## Run

```bash
go run main.go
```

Writes a temperature reading and queries the mean over 5-minute windows.
